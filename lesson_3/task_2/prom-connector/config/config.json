{
  "name": "prometheus-connector",
  "config": {
    "topics": "test-topic",
    "prometheus.listener.url": "http://localhost:8080/metrics",
    "tasks.max": "1",
    "connector.class": "PrometheusSinkConnector"
  }
}